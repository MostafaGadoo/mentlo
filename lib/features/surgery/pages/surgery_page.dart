import 'package:flutter/material.dart';
import 'package:mentlo/features/surgery/widgets/surgery_widget.dart';

class SurgeryPage extends StatelessWidget {
  const SurgeryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SurgeryWidget();
  }
}
